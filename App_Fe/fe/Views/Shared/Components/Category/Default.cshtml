@model List<Models.CategoryModel>

<div class="col-lg-12">
    <div class="mb-3">
        <h4>Danh mục</h4>
        <ul class="list-unstyled fruite-categorie">
            @foreach (var i in Model)
            {
                <li>
                    <div class="d-flex justify-content-between fruite-name">
                        <a asp-controller="Products" asp-action="Index" asp-route-CategoryId="@i.CategoryId"
                            style="@(ViewBag.CurrentCateroryID == i.CategoryId ? "text-decoration: underline;" : "")">@i.Name</a>
                        <span></span>
                    </div>
                </li>
            }
        </ul>
    </div>
</div>